// Canonical list of expense recipients (the named cash people from the Daily
// Expenses Report). Selecting "Other" lets the cashier type a custom name.
export const EXPENSE_RECIPIENTS = ["Junaid", "Usama"] as const;
